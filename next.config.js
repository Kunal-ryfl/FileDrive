/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "uploadthing.com",
      "utfs.io",
      "user-images.githubusercontent.com"
    ]
  }
}

module.exports = nextConfig
